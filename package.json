{
  "name": "knockout-chooose",
  "version": "0.4.5",
  "description": "A lightweight and powerful select alternative to let users choose items from many options",
  "main": "choose.js",
  "devDependencies": {
    "chai": "^3.2.0",
    "chai-dom": "^1.0.0",
    "grunt": "^0.4.5",
    "grunt-contrib-connect": "^0.11.2",
    "grunt-saucelabs": "^8.6.1",
    "knockout-css3-animation": "^0.1.1",
    "mocha": "^2.2.5",
    "mocha-phantomjs": "^4.0.0",
    "sinon-browser-only": "^1.12.1",
    "sinon-chai": "^2.8.0"
  },
  "scripts": {
    "test": "npm install knockout@3.3.0 && mocha-phantomjs tests/tests.html && (node -e \"require('grunt').tasks(['test']);\" || true)"
  },
  "keywords": [
    "select",
    "choose",
    "options",
    "knockout",
    "knockout-plugin",
    "knockout-component"
  ],
  "author": "Nathan Black <nathan@nathanblack.org>",
  "license": "ISC",
  "dependencies": {
    "knockout": "^3.2.0",
    "aria-listbox": "^0.1.2"
  }
}
